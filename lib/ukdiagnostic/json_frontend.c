#include <uk/json_frontend.h>
#include <stdio.h>
#include <stdlib.h>





static struct json_value* parse_value(const char* data, size_t len, size_t pos) {
   return NULL;
}


struct json_value* parse_json(const char* data, size_t len) {
    printf("Testing\n");

    


    return parse_value(data, len, 0);
}
